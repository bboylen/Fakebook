<%= form_with(url: comments_path, method: :post) do |form|%>
  <%= form.text_area :body, style: "width:100%", placeholder: "Write a comment..."%>
  <%= form.hidden_field :post_id, value: post_id %>
  <%= form.hidden_field :author_id, value: author_id %>
  <%= form.submit "Comment"%>
<% end %>