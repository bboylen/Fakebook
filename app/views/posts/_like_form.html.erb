<%= form_with(:url => like_post_path(post_id), method: "post", local: true) do |form| %>
  <div class="actions">
    <%= form.hidden_field :user_id, :value => user_id %>
    <%= form.submit "Like"%>
  </div>
<% end %>
