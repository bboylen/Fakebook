
<h1>Timeline</h1>
<br>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.author.first_name%> <%= post.author.first_name%></td>
      </tr>
      <tr>
        <td><%= post.body%></td>
      </tr>
      <tr>
        <td><%= link_to 'Show', post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
