<%= form_with(url: set_profile_picture_user_path, local: true) do |form| %>
  <div class="actions">
    <%= form.label "Profile Picture"%>
    <%= form.file_field :profile_picture%>
    <%= form.submit %>
  </div>
<% end %>
